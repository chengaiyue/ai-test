# ============================================================================
# 业务应用：脚本 / 批处理服务（不对外发布）
#   - package = false：源码直接运行，不构建不发布
#   - 适合定时任务、数据处理、CLI 工具等
# ============================================================================

[project]
name = "worker-app"
version = "0.1.0"
description = "脚本 / 批处理服务（内部使用，不发布）"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["common-utils"]

[tool.uv]
package = false
